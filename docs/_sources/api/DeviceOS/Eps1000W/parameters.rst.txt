Eps1000W Parameters
===================

.. admonition:: Parameter reference
   :class: api-reference

   .. list-table::
      :header-rows: 1
      :width: 100%
      :class: api-table api-parameter-table
      :widths: 20 15 10 15 12 28

      * - Name
        - Type
        - Unit
        - Range
        - Default
        - Description
      * - ``classicOutputAttenuation``
        - ``float``
        - ``dB``
        - —
        - —
        - Classic-output attenuation.
      * - ``clockAttenuation``
        - ``float``
        - ``dB``
        - —
        - —
        - Clock-output attenuation.
      * - ``clockFrequency``
        - ``float``
        - ``MHz``
        - —
        - —
        - Clock-output frequency.
      * - ``clockOutput``
        - ``object``
        - —
        - —
        - —
        - Set EPS clock output with one k-menu transaction: frequency first, attenuation second.
      * - ``entangledOutputAttenuation``
        - ``float``
        - ``dB``
        - —
        - —
        - Entangled-output attenuation.
      * - ``outputMode``
        - ``str``
        - —
        - ``HH, DD, ENTANGLED``
        - —
        - EPS output mode.
      * - ``pumpFrequency``
        - ``float``
        - ``MHz``
        - —
        - —
        - Pump frequency.
      * - ``pumpPhase``
        - ``float``
        - ``ps``
        - —
        - —
        - Pump phase.
      * - ``referenceFrequency``
        - ``float``
        - ``MHz``
        - —
        - —
        - Reference frequency.
      * - ``splittingRatioPercent``
        - ``int``
        - ``%``
        - ``0 to 100``
        - —
        - Output splitting ratio.

   .. rubric:: Examples
      :class: examples-heading

   A running micro-protocol must include the target ``deviceId`` in its ``resourceRequests``. NodeEngine retrieves driver capabilities during the device handshake and verifies that the requested parameter is advertised.

   Use the parameter table to choose an advertised parameter and a valid value.

   Replace each ``<...>`` placeholder with the appropriate device ID, advertised parameter name, or valid parameter value.

   .. rubric:: Read a parameter from a Python handler
      :class: invocation-heading

   .. code-block:: python

      result = await ctx.get_parameter(
          deviceId="<device-id>",
          name="<parameter-name>",
      )

   .. rubric:: Write a parameter from a Python handler
      :class: invocation-heading

   .. code-block:: python

      result = await ctx.set_parameter(
          deviceId="<device-id>",
          name="<parameter-name>",
          value="<parameter-value>",
      )


.. note::
   :class: parameter-note

   Numeric min/max limits and defaults are deployment-specific: they are read from this EPS device's ``hardwareConfig``. Network JSON configurations provide them through ``hardwareConfig.parameterLimits`` and the parameter values. Only ``min`` and ``max`` are advertised by the driver; ``splittingRatioPercent`` defaults to the built-in 0 to 100 range.
