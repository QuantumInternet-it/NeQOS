Mpc320 Foreground Tasks
=======================

.. _deviceos-mpc320-setparameters:

.. admonition:: ``SetParameters``
   :class: driver-overview task-reference

   Apply optional polarizer configuration overrides.

   The task holds the driver I/O lock while it calls the vendor API. An
   argument set to ``None`` keeps the corresponding configured value.
   On success, the result contains the resolved serial number and all
   five applied polarizer settings. A cancelled context returns
   ``CANCELLED``; a rejected or unavailable device raises ``RuntimeError``.

   :Task class: Foreground
   :Admission policy: ``sync``
   :Pausable: No

   .. list-table::
      :header-rows: 1
      :width: 100%
      :class: api-table api-task-parameter-table
      :widths: 14 23 10 14 39

      * - Parameter
        - Type
        - Required
        - Default
        - Description
      * - ``velocity``
        - ``float \| int \| None``
        - No
        - ``None``
        - —
      * - ``home_position``
        - ``float \| int \| None``
        - No
        - ``None``
        - —
      * - ``jog_size1``
        - ``float \| int \| None``
        - No
        - ``None``
        - —
      * - ``jog_size2``
        - ``float \| int \| None``
        - No
        - ``None``
        - —
      * - ``jog_size3``
        - ``float \| int \| None``
        - No
        - ``None``
        - —
      * - ``serialNumber``
        - ``str \| None``
        - No
        - ``None``
        - —

   :Result keys: ``home_position``, ``jog_size1``, ``jog_size2``, ``jog_size3``, ``reason``, ``serialNumber``, ``status``, ``velocity``

   .. rubric:: Examples
      :class: examples-heading

   The enclosing micro-protocol must include the target ``deviceId`` in its ``resourceRequests``. NodeEngine retrieves driver capabilities during the device handshake and verifies that the task is advertised.

   Replace each ``<...>`` placeholder with a value accepted by the corresponding parameter.

   .. rubric:: Static task invocation from a micro-protocol declaration
      :class: invocation-heading

   .. code-block:: json

      {
        "stageId": "<stage-id>",
        "handler": "RunSingleTaskStage",
        "params": {
          "deviceId": "<device-id>",
          "taskName": "SetParameters",
          "taskParams": {}
        }
      }

   .. rubric:: Dynamic task invocation from a Python handler
      :class: invocation-heading

   .. code-block:: python

      reply = await ctx.run_task(
          deviceId="<device-id>",
          taskName="SetParameters",
          params={
          },
      )


.. _deviceos-mpc320-moveto:

.. admonition:: ``MoveTo``
   :class: driver-overview task-reference

   Move one paddle to an absolute target angle.

   The task holds the driver I/O lock for both the movement and its
   position readback. When ``timeout`` is omitted it uses
   ``operation_timeout``. The target angle is constrained to the
   configured 0--160 degree range. A requested displacement smaller than
   ``minimum_step`` is accepted without issuing a vendor movement.

   The result contains ``status``, the resolved serial number, normalized
   paddle index, requested target angle, reported position, and effective
   timeout. Movement failures are reported with ``status: ERROR``;
   a cancelled context returns ``CANCELLED``.

   :Task class: Foreground
   :Admission policy: ``async``
   :Pausable: No

   .. list-table::
      :header-rows: 1
      :width: 100%
      :class: api-table api-task-parameter-table
      :widths: 14 23 10 14 39

      * - Parameter
        - Type
        - Required
        - Default
        - Description
      * - ``angle``
        - ``float``
        - Yes
        - —
        - —
      * - ``paddle``
        - ``int``
        - Yes
        - —
        - —
      * - ``timeout``
        - ``float \| None``
        - No
        - ``None``
        - —
      * - ``serialNumber``
        - ``str \| None``
        - No
        - ``None``
        - —

   :Result keys: ``paddle``, ``position``, ``reason``, ``serialNumber``, ``status``, ``target_angle``, ``timeout``

   .. rubric:: Examples
      :class: examples-heading

   The enclosing micro-protocol must include the target ``deviceId`` in its ``resourceRequests``. NodeEngine retrieves driver capabilities during the device handshake and verifies that the task is advertised.

   Replace each ``<...>`` placeholder with a value accepted by the corresponding parameter.

   .. rubric:: Static task invocation from a micro-protocol declaration
      :class: invocation-heading

   .. code-block:: json

      {
        "stageId": "<stage-id>",
        "handler": "RunSingleTaskStage",
        "params": {
          "deviceId": "<device-id>",
          "taskName": "MoveTo",
          "taskParams": {
            "angle": "<float>",
            "paddle": "<int>"
          }
        }
      }

   .. rubric:: Dynamic task invocation from a Python handler
      :class: invocation-heading

   .. code-block:: python

      reply = await ctx.run_task(
          deviceId="<device-id>",
          taskName="MoveTo",
          params={
              "angle": "<float>",
              "paddle": "<int>",
          },
      )


.. _deviceos-mpc320-moverelative:

.. admonition:: ``MoveRelative``
   :class: driver-overview task-reference

   Move one paddle by a relative angle.

   The task holds the driver I/O lock for both the movement and its
   position readback. When ``timeout`` is omitted it uses
   ``operation_timeout``. Relative requests above 160 degrees in
   magnitude are rejected. If a target is within 2 degrees of either
   hardware boundary, it is clamped to the 0--160 degree range; targets
   further outside that range are rejected. A displacement smaller than
   ``minimum_step`` is accepted without issuing a vendor movement.

   The result contains ``status``, the resolved serial number, normalized
   paddle index, requested relative angle, reported position, and
   effective timeout. Movement failures are reported with ``status: ERROR``;
   a cancelled context returns ``CANCELLED``.

   :Task class: Foreground
   :Admission policy: ``async``
   :Pausable: No

   .. list-table::
      :header-rows: 1
      :width: 100%
      :class: api-table api-task-parameter-table
      :widths: 14 23 10 14 39

      * - Parameter
        - Type
        - Required
        - Default
        - Description
      * - ``relative_angle``
        - ``float``
        - Yes
        - —
        - —
      * - ``paddle``
        - ``int``
        - Yes
        - —
        - —
      * - ``timeout``
        - ``float \| None``
        - No
        - ``None``
        - —
      * - ``serialNumber``
        - ``str \| None``
        - No
        - ``None``
        - —

   :Result keys: ``paddle``, ``position``, ``reason``, ``relative_angle``, ``serialNumber``, ``status``, ``timeout``

   .. rubric:: Examples
      :class: examples-heading

   The enclosing micro-protocol must include the target ``deviceId`` in its ``resourceRequests``. NodeEngine retrieves driver capabilities during the device handshake and verifies that the task is advertised.

   Replace each ``<...>`` placeholder with a value accepted by the corresponding parameter.

   .. rubric:: Static task invocation from a micro-protocol declaration
      :class: invocation-heading

   .. code-block:: json

      {
        "stageId": "<stage-id>",
        "handler": "RunSingleTaskStage",
        "params": {
          "deviceId": "<device-id>",
          "taskName": "MoveRelative",
          "taskParams": {
            "relative_angle": "<float>",
            "paddle": "<int>"
          }
        }
      }

   .. rubric:: Dynamic task invocation from a Python handler
      :class: invocation-heading

   .. code-block:: python

      reply = await ctx.run_task(
          deviceId="<device-id>",
          taskName="MoveRelative",
          params={
              "relative_angle": "<float>",
              "paddle": "<int>",
          },
      )

